#include<climits>
#include<iostream>
#include<bits/stdc++.h>
#include<algorithm>
#include<vector>
#include<map>


#define INF 1111111111
#define SIZE 100
#define MOD 1000000007
#define llong long long
#define f1(i,n) for(i=1;i<=n;i++)
#define f0(i,n) for(i=0;i< n;i++)
using namespace std;

bool chdir(string P,string A)
{
      if(A == ".") return true;
      if(A == "..")
      {     if(P.size()==1)return false;
            char c;
            while(P[P.size()-1]!= '/')
                  P.pop_back();
            if(P.)
      } 
      
}
int main(int argc, char *argv[])
{
      string S("/home/pawan/DRIVE/CODES/projs/DIRECTORY");
      

     return 0;
}
